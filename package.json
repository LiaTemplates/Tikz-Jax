{
  "name": "tikz",
  "version": "1.0.0",
  "scripts": {
    "watch": "npx parcel serve src/index.ts --open",
    "build": "npx parcel build src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "parcel": "^2.12.0"
  }
}
